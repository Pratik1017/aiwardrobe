#!/bin/bash
# MongoDB Setup Helper for AL Closet

echo "MongoDB Setup Options for AL Closet"
echo "======================================"
echo ""
echo "Option 1: MongoDB Atlas (Cloud - Recommended & Free)"
echo "-----------------------------------------------------"
echo "1. Go to https://www.mongodb.com/cloud/atlas"
echo "2. Sign up for a free account"
echo "3. Create a free cluster"
echo "4. Get your connection string (looks like):"
echo "   mongodb+srv://username:password@cluster.mongodb.net/al-closet"
echo "5. Update .env file MONGODB_URI with your connection string"
echo ""
echo "Option 2: Local MongoDB Installation (Windows)"
echo "-----------------------------------------------"
echo "1. Download from: https://www.mongodb.com/try/download/community"
echo "2. Run the installer (mongodb-windows-x86_64-xxx.msi)"
echo "3. Choose 'Install as a Service' (default)"
echo "4. MongoDB will start automatically"
echo "5. Connection string: mongodb://localhost:27017/al-closet"
echo ""
echo "Option 3: Docker (if Docker is installed)"
echo "------------------------------------------"
echo "docker run -d -p 27017:27017 --name mongodb mongo"
echo ""
echo "Then update your .env MONGODB_URI accordingly."
